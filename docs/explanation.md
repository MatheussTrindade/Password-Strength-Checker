# Ferramenta web para análise de segurança de senhas baseada em heurísticas comuns de ataques reais

# COMO RODAR

* pip install flask
* python run.py

# IMPORTANTE

Isso aqui:
* verify=False
apenas para desenvolvimento
Em produção
Recomendado instalar certificados:
* pip install certifi
E usar:
* verify=certifi.where()